#include <bits/stdc++.h>
using namespace std;

int main() {
    // 40点解法
    int N, H, A, B, C, D, E;
    cin >> N >> H >> A >> B >> C >> D >> E;

    vector<map<int, long long>> dp(N+1);
    dp[0][H] = 0;
    for (int i = 0; i < N; i++) {
        // ここから書く
    }
}